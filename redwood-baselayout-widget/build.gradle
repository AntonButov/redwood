import static app.cash.redwood.buildsupport.TargetGroup.Common

redwoodBuild {
  targets(Common)
  publishing()
}

apply plugin: 'app.cash.redwood.generator.widget'

kotlin {
  sourceSets {
    commonMain {
      dependencies {
        api projects.redwoodBaselayoutModifiers
      }
    }
  }
}

redwoodSchema {
  source = projects.redwoodBaselayoutSchema
  type = 'app.cash.redwood.baselayout.RedwoodBaseLayout'
}
